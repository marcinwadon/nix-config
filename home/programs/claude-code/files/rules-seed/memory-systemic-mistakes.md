# Systemic mistakes

If you ever notice a mistake committed or deployed, ask **"how could we have
systemically caught issues like this"** and prompt it to the user. The goal is
to identify the missing guardrail (a test, lint rule, type, schema, CI check)
that would have caught this class of issue automatically. Populated per-machine
via the auto-update memory rule.
