# PLACEHOLDER — replace at deploy with a real sops file encrypted to the monitor
# CT's age key: `sops secrets/monitor.yaml` then add `monitor_token: <value>`.
# (See docs/RUNBOOK-lxc.md.) validateSopsFiles=false lets this eval pre-deploy.
monitor_token: REPLACE_AT_DEPLOY
